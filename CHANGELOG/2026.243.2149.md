# 2026.243.2149 — docs(skills): tighten the 8 automation skill/agent descriptions to trigger-style

## Summary

Tighten the `description:` frontmatter of all 8 charly-automation entities (7 skills + 1 agent) to trigger-style index lines ("Use when X"). The descriptions are the skill/agent index injected into every harness system prompt (progressive disclosure: only name + description + location are always in context). Shorter, trigger-style descriptions shrink the index without touching names, bodies, or discoverability — **zero capability reduction**: all 8 entities remain discoverable and loadable.

Description bytes: ~2,200 → 1,714.

## How tested

- `python3` YAML parse of the edited `charly.yml`: OK — all 8 entities parse, descriptions verified.
- `git diff` review: only `description:` blocks changed (15 insertions / 9 deletions); no structural key (`name`/`family`/`owner`/`triggers`/`content`/`references`/`model`) touched; no body content changed.
- The change is metadata-only prose; no behavior, schema, or build surface affected.

## Rulebook compliance

- **R3** — no duplication: each description is a single canonical index line.
- **R5** — no stale references: the new descriptions keep the same routing signal.
- No pins, no scripts, no schema changes, no direct push to main.

## Change classification

- **Change class:** docs-only (skill/agent-description metadata prose; zero behavior change)
- **Verification gate:** YAML parse + diff review
- **Attribution tier:** `documentation reviewed`

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*