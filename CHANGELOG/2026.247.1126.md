# 2026.247.1126 — feat: crabbox-deploy automation skill (crabbox wave, Phase 3)

## Summary

Adds the `crabbox-deploy` automation skill entity (family `automation`, owner
`charly-automation`) to this repo's concept candy — mirroring `openclaw-deploy`.
Phase 3 of the 100%-local Crabbox integration (plan/crabbox-integration.md):

- Documents the Crabbox CLI + the local Node/PostgreSQL coordinator + the
  `crabbox:` check verb as a composable, fully-local control plane.
- Quick reference (CLI commands, shared-token login, verb methods),
  the candy surface (`layer-crabbox` v2026.247.0855 / `pod-crabbox`
  v2026.247.1016 / `plugin-crabbox` v2026.247.1021), the distro boxes/beds,
  and the recorded local-loop routing to the named batch
  `plan/crabbox-loop-cutover.md` (distro-cachyos).

Also migrates the repo root schema to 2026.240.1943 (fresh-binary floor).

## How tested

```
$ charly box validate
box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors

# the skill entity is a generated-corpus input: after pod-charly-automation's
# next tag, marketplace regeneration emits automation/skills/crabbox-deploy
```

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | skills/marketplace + layer skills loaded before authoring. |
| R1 — RCA every anomaly | Schema-floor failure RCA'd (fresh-binary floor) and fixed via the sanctioned `charly migrate`; `box validate` 0/0. |
| R2 — finish the whole cutover | The skill + the schema migration ship in ONE PR. |
| R3 — no duplication | Mirrors the openclaw-deploy entity shape; crab-specific content only. |
| R4 — no workarounds | `charly migrate` is the sanctioned path, not a hand edit. |
| R5 — delete legacy | N/A. |
| R6 — git safety | No force-push; branch-per-change PR. |
| R7 — prove behaviour | Manifest + entity validated at the fresh-binary gate (0/0); corpus emission verified at Phase 4. |
| R8 — preserve emitted artifacts | Skill entity validated as a generated-corpus input (0/0). |
| R9 — binary equals source | Fresh worktree-built bin/charly used. |
| R10 — fresh disposable proof | N/A — docs/skill-only change (no runtime path); the crabbox beds carry the R10 (distro-cachyos #62). |
| RDD/ADE/SDD | Skill entity + description authored with the change; no spec changes (N/A SDD). |
| Concurrency | N/A. |
| Disposable-only gate | N/A — no targets created/destroyed (docs-only). |
| Hooks doctrine | N/A standalone repo; merge gate = org pr-validator. |

## Change classification

- **Class:** documentation/skill (automation corpus input).
- **Verification gate:** `charly box validate` (CI + fresh binary), 0/0.
- **Attribution:** documentation reviewed + validated (skill corpus input; the
  runtime surface is bed-proven in distro-cachyos #62).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed + validated)*