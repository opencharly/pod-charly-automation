# 2026.242.1921 — chore: advance the layer-sway-desktop pin to v2026.242.1839

## Summary

`layer-sway-desktop` fixed four stale pins that declared dependencies as **bare names**
instead of full `@github` refs (opencharly/layer-sway-desktop#3), taking its own
`charly box validate` from exit 1 to exit 0.

This repo still pinned `v2026.238.2103`, which predates that. Left alone it drags the old
transitive pins of `layer-xdg-portal` and `layer-chrome-sway` back into any project that
composes this candy, which charly reports on every build:

```
Warning: candy github.com/opencharly/layer-xdg-portal resolved to multiple versions;
using newest 2026.242.1655 (from …@v2026.242.1648), ignoring 2026.144.1443 (from …@v2026.237.557)
```

**Measured, not assumed.** In a downstream project, the count was 0 before any consumer
pinned `layer-xdg-portal`, 3 after one did, and still 3 after repinning every *direct*
consumer — which identified the survivors as transitive. After repinning
`layer-sway-desktop` itself to `v2026.242.1839`, the remaining old refs were attributed
specifically to `layer-sway-desktop@v2026.238.2103`, i.e. this pin.

### The diff, in full

Two lines change in `charly.yml`:

```
- '@github.com/opencharly/layer-sway-desktop:v2026.238.2103'   ->  v2026.242.1839
  version: 2026.218.1200                                       ->  2026.242.1845
```

The second is this candy's OWN version, bumped because any change to a candy's content
needs a new CalVer for consumers to pin — a ref bump that left the version untouched would
publish different content under a version already released. `2026.242.1845` is the standard
`YYYY.DDD.HHMM` stamp at the time of the edit (day 242, 18:45), matching the convention
every other candy in the org uses; it carries no meaning beyond ordering.

## How tested

```
$ charly box validate
exit 0
```

Run with charly **`v2026.238.1242`**, the version this repo's `deploy.yml` clones and
builds, so it is the same gate CI runs rather than a newer local binary. The exit code was
read directly from `charly box validate`, not through a pipeline.

The consuming project's zero-warning bed run lands with the repin that follows this tag —
the same ordering constraint that applied to `layer-sway-desktop#3`: a transitive pin can
only stop being resolved once the repo removing it is merged and tagged, so no bed run in
*this* repo can demonstrate the drop.

## Rulebook compliance

- **R0 (skills)** — `/charly-image:layer` for the candy/ref model; `/charly-internals:git-workflow`
  for PR-only landing and tag-on-merge.
- **R1 (RCA)** — the skew edge was located by counting warnings across runs while repinning
  consumers one at a time, not by bumping pins until the message stopped.
- **R2 (no deferral)** — this is the follow-through on a cascade found while doing other work,
  fixed in the repo that owns the pin rather than labelled pre-existing downstream.
- **R3 (no duplication)** — one pin, advanced in place.
- **R4 (no workarounds)** — the pin is advanced rather than the warning allowlisted.
- **R5 (hard cutover)** — a pin value changes; no old path is left behind. Sweep actually
  executed, output pasted, not asserted:

  ```
  $ grep -rn "layer-sway-desktop:v" . --include="*.yml"
  charly.yml: - '@github.com/opencharly/layer-sway-desktop:v2026.242.1839'
  ```

  One reference, at the new pin. A broader sweep for the bare tag string `v2026.238.2103`
  also matches `layer-openclaw-full` in one repo of this cascade — a DIFFERENT candy that
  happens to share the tag string, not a surviving `layer-sway-desktop` reference. Worth
  stating because an unqualified "grep returns nothing" would have been false there.
- **R6 (git safety)** — feature branch, no force-push, no hook bypass.
- **R7 / R10 (prove the gate)** — `charly box validate` run on the committed tree with the
  repo's own pinned charly.
- **R8 (generation)** — N/A: no generated artifact, no CUE input in this repo.
- **R9 (artifact integrity)** — N/A: no image is built here.
- **ADE (candy description/plan)** — N/A: `description:` and `plan:` untouched, nothing
  re-projects.
- **SDD (CUE source)** — N/A: no schema authored or generated here.

## Change classification

- **Change class:** candy ref pin bump + the candy's own CalVer bump. No packages, services,
  plan steps or scripts change.
- **Verification gate:** `charly box validate` under charly `v2026.238.1242`, exit 0
- **Attribution tier:** `analysed on a live system` — the gate was executed on a real checkout
  with the exit code recorded, and the skew this removes was counted in real bed builds; no
  image is built in this repo

*Assisted-by: Claude Code Anthropic Opus 5 (analysed on a live system)*