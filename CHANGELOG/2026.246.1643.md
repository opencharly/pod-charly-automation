# 2026.246.1643 — docs(skills): OpenClaw 2.0 updates to the openclaw-deploy skill

## Summary

Update the `openclaw-deploy` skill for **OpenClaw 2.0** (npm `openclaw@2026.8.x`):

- **Model routes unified:** `openai-codex/gpt-5.4` → `openai/gpt-5.4` and
  `--provider openai-codex` → `--provider openai` (2.0 unified the `codex/*` and
  `openai-codex/*` routes under `openai/*`).
- **Container probes:** the Health Checks section now lists `/startupz` alongside
  `/healthz` and `/readyz` (the 2.0 container-probe endpoints).
- **2.0 migration notes:** a new section documents the operator-facing breaking
  changes — the route unification, OpenProse retirement, the dead
  chrome-extension-relay config (`driver: "extension"` / `browser.relayBindHost`),
  and the `openclaw doctor --fix` migrations (QMD memory, `execSecurity`/`execAsk`,
  `HEARTBEAT.md`) — and confirms the gateway startup surface is unchanged.

The candy's own CalVer moves because changed content must ship under a new version.

## How tested

```
$ /tmp/charly-ci/bin/charly box validate
exit 0
```

Run with the charly version this repo's own `deploy.yml` clones and builds
(v2026.238.1242). The diff is confined to `charly.yml`: the skill content and the
version line. This is a documentation-only candy (no install content), so there is
no runtime path to exercise. The marketplace corpus regeneration (which consumes
this skill) lands in the marketplace repo once this tag is published.

## Rulebook compliance

- **R0 (skills)** - `/charly-internals:skills` for skill maintenance;
  `/charly-internals:git-workflow` for PR-only landing and tag-on-merge.
- **R1 (RCA)** - the 2.0 breaking changes were researched before editing: the route
  unification, OpenProse retirement, and doctor --fix migrations were verified
  against the OpenClaw 2.0 release notes.
- **R2 (no deferral)** - the skill ships the 2.0 guidance now, in the same batch as
  the gateway bump.
- **R3 (no duplication)** - the skill is the single source; the marketplace corpus is
  regenerated from it.
- **R4 (no workarounds)** - N/A.
- **R5 (hard cutover)** - the stale `openai-codex/*` references are gone.
- **R6 (git safety)** - feature branch, no force-push, no hook bypass.
- **R7 / R10 (prove the gate)** - `charly box validate` on the committed tree with the
  repo's own pinned charly.
- **R8 (generation)** - the marketplace corpus is regenerated from this source in the
  marketplace repo (same batch, after the tag).
- **R9 (artifact integrity)** - N/A: no image is built here.
- **ADE (candy description/plan)** - N/A: `description:` and `plan:` untouched (the
  change is in a skill entity's content).
- **SDD (CUE source)** - N/A: no schema authored or generated here.

## Change classification

- **Change class:** skill content update + the candy's own CalVer bump. Docs only.
- **Verification gate:** `charly box validate` under the repo's pinned charly, exit 0
- **Attribution tier:** `documentation reviewed` - docs-only change class; the gate
  was executed on a real checkout with the exit code recorded

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*